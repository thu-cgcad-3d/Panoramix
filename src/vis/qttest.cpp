#include "qttest.hpp"

#include "core/feature.hpp"

namespace panoramix {
    namespace vis {

        TestWidget::TestWidget(QWidget * parent) : QGLWidget(parent) {
        	
        }
        
        TestWidget::~TestWidget() {
        	
        }

    }
}