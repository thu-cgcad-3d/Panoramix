
#version 150

uniform mat4 worldViewProj;

in vec4 vertex;
in vec4 normal;

out vec3 pixelPosition;
out vec3 pixelNormal;
out vec4 position;

void main(void)
{
    pixelPosition = vertex.xyz / vertex.w;
    pixelNormal = normal;
    position = worldViewProj * vertex;
}